{
  "name": "arcadia-zoo",
  "version": "1.0.0",
  "description": "Arcadia Zoo webapp, ECF for TP DWWM",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm run install-client && npm run install-server",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "dev": "concurrently -c \"yellow.bold,blue.bold\" -n \"Server,Client\" \"npm run dev --prefix server\" \"cd client && ng serve\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thomasphilipps/arcadia.git"
  },
  "author": "Thomas Philipps",
  "license": "CC-BY-NC-SA-4.0",
  "bugs": {
    "url": "https://github.com/thomasphilipps/arcadia/issues"
  },
  "homepage": "https://github.com/thomasphilipps/arcadia#readme",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
